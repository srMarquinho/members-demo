<h1><%= @school.name %></h1>
<%= link_to "Edit", edit_school_path(@school) %> | <%= link_to "Delete", school_path(@school), method: :delete %>

<h3>Members</h3>
  <ul>
    <% @school.members.each do |member| %>
      <li><%= link_to member.name, member_path(member) %></li>
    <% end %>
  </ul>
